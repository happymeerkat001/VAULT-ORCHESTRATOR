Run the Hedy sync workflow:

```bash
python3 /Users/leon/Documents/Code/Obsidian-vault-orchestrator/ingest/hedy_sync.py
```

Print stdout/stderr exactly as returned. Do not reformat or summarize.
If the script exits non-zero, tell the user: "Hedy sync failed. Check the error above and verify `hedy_api_key` in `~/.config/vault-orchestrator/google_credentials`."
